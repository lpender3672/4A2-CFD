from setuptools import setup, find_packages

setup(
    name="4A2-CFD",  # Replace with your project's name
    version="0.1",
    packages=find_packages(),  # Automatically includes subfolders with __init__.py
)